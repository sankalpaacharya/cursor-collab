packages:
  - server
  - client

# Allow these dependencies to run install scripts (pnpm >= 10.15 reads this here;
# the package.json "pnpm" field is ignored by pnpm >= 11).
onlyBuiltDependencies:
  - esbuild
