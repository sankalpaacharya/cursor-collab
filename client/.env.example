# Backend URL the client connects to.
# - Leave EMPTY to use the Vite dev proxy / same-origin (recommended for local dev).
# - Set to the load balancer URL in production, e.g. https://cursors.example.com
VITE_SERVER_URL=
